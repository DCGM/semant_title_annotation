# Quick Start (Frontend)

```bash
npm install
npm run dev
```

Set backend URL through `API_URL` (default currently from axios boot file).

Primary user flow:
1. Login/register
2. Select up to 6 tasks
3. Classify texts
4. Inspect leaderboard
